services:
  dapp-example:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_1AM_NETWORK: ${VITE_1AM_NETWORK:-preview}
    container_name: dapp-example
    ports:
      - "5173:5173"
    restart: unless-stopped
