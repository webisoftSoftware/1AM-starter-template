// Generated by scripts/generate-proof-simulator.mjs. Do not hand-edit.
pragma language_version 0.23;

import CompactStandardLibrary;

export ledger lastValue: Field;

constructor() {
  lastValue = 0;
}

circuit mix(accumulator: Field, value: Field): Field {
  return accumulator * accumulator + value;
}

export circuit probeK06(seed: Vector<16, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK07(seed: Vector<32, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK08(seed: Vector<64, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK09(seed: Vector<128, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK10(seed: Vector<256, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK11(seed: Vector<512, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK12(seed: Vector<1024, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK13(seed: Vector<2048, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK14(seed: Vector<4096, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK15(seed: Vector<8192, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK16(seed: Vector<16384, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK17(seed: Vector<32768, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK18(seed: Vector<65536, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK19(seed: Vector<131072, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}

export circuit probeK20(seed: Vector<262144, Field>): [] {
  lastValue = disclose(fold(mix, 0, seed));
}
